# AGPL Vendor Zone

This directory should remain empty for the Apache-2.0 core project.

AGPL/GPL components must not be vendored into the core source tree. Use dependency-based optional backends
or a separate isolated distribution only after explicit license review.
